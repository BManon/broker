export enum Qualification {
  ISO9001,
  ISO14001,
  ISO10303
}

export enum Role {
  PRODUCTION,
  TRANSPORT
}

export enum Ability {
  ASSEMBLY_ASSISTANT,
  TRANSPORT_COOLING
}

export enum Machine {
  SMALL_SIMPLEX,
  SMALL_DUPLEX,
  SMALL_SUPPORT,
  LARGE_SIMPLEX,
  LARGE_DUPLEX,
  LARGE_SUPPORT
}